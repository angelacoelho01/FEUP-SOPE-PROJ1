#include "permissions.h"

int change_permissions(const char *path) {
	// the act of be able to change permissions is done by 
	// writing the path of the content that we acess
	printf(" %s \n", path);
	
	return 0;
}
